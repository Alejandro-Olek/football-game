<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Retro Fútbol - Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
  <style>
    /* Estilos globales */
    body {
      background-color: #1a202c; /* Gray-900 */
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      font-family: sans-serif;
    }

    /* Contenedor del formulario */
    .login-container {
      width: 100%;
      max-width: 448px; /* max-w-md */
      background-color: #2d3748; /* Gray-800 */
      padding: 2rem; /* p-8 */
      border-radius: 0.5rem; /* rounded-lg */
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
                  0 4px 6px -2px rgba(0, 0, 0, 0.05); /* shadow-lg */
      border: 4px solid #48bb78; /* border-4 border-green-500 */
      box-sizing: border-box;
    }

    /* Título */
    .login-container h1 {
      font-size: 1.5rem; /* text-2xl */
      text-align: center;
      font-weight: bold;
      color: #68d391; /* text-green-400 */
      font-family: 'Press Start 2P', cursive; /* font-retro */
      margin: 0;
    }

    /* Formulario */
    .login-form {
      margin-top: 1.5rem; /* mt-6 */
    }

    .login-form label {
      display: block; /* block */
      margin-bottom: 0.5rem; /* mb-2 */
      color: #9ae6b4; /* text-green-300 */
      font-family: 'Press Start 2P', cursive; /* font-retro */
    }

    .login-input {
      width: 100%; /* w-full */
      padding: 0.5rem; /* p-2 */
      border-radius: 0.25rem; /* rounded */
      background-color: #4a5568; /* bg-gray-700 */
      color: #fff; /* text-white */
      border: 1px solid #68d391; /* border border-green-400 */
      outline: none;
      box-sizing: border-box;
    }

    .login-input:focus {
      box-shadow: 0 0 0 2px #63b3ed; /* focus:ring-2 focus:ring-blue-400 */
    }

    .login-button {
      width: 100%; /* w-full */
      margin-top: 1.5rem; /* mt-6 */
      background-color: #48bb78; /* bg-green-500 */
      color: #1a202c; /* text-gray-900 */
      font-weight: bold; /* font-bold */
      padding: 0.5rem 1rem; /* py-2 px-4 */
      border-radius: 0.5rem; /* rounded-lg */
      border: none;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out; /* transition-all */
    }

    .login-button:hover {
      background-color: #68d391; /* hover:bg-green-400 */
    }

    .login-paragraph {
      text-align: center; /* text-center */
      margin-top: 1rem; /* mt-4 */
      color: #63b3ed; /* text-blue-400 */
      font-family: 'Press Start 2P', cursive; /* font-retro */
    }

    .login-paragraph a {
      text-decoration: underline;
      color: #63b3ed;
    }

    .login-paragraph a:hover {
      color: #90cdf4; /* hover:text-blue-300 */
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>⚽ Retro Fútbol Login</h1>
    
    <!-- Formulario de Login -->
    <form action="/login" method="POST" class="login-form">
      <label for="email">Correo Electrónico</label>
      <input type="email" id="email" name="email" required class="login-input">
      
      <label for="password" style="margin-top: 1rem;">Contraseña</label>
      <input type="password" id="password" name="password" required class="login-input">
      
      <button type="submit" class="login-button">Iniciar Sesión</button>
    </form>
    
    <!-- Enlace para registrarse -->
    <p class="login-paragraph">
      ¿No tienes cuenta? <a href="/auth/register">Regístrate</a>
    </p>
  </div>
</body>
</html>