<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Retro Football - Register</title>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
  <style>
    /* Estilos globales */
body {
  background-color: #1a202c; /* Gray-900 */
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  margin: 0;
  font-family: sans-serif;
}

/* Contenedor del registro */
.register-container {
  width: 100%;
  max-width: 448px; /* max-w-md (28rem = 448px) */
  background-color: #2d3748; /* Gray-800 */
  padding: 2rem; /* p-8 */
  border-radius: 0.5rem; /* rounded-lg */
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
              0 4px 6px -2px rgba(0, 0, 0, 0.05); /* shadow-lg */
  border: 4px solid #4299e1; /* border-4 y blue-500 */
  box-sizing: border-box;
}

/* Título */
.register-container h1 {
  font-size: 1.5rem; /* text-2xl */
  text-align: center;
  font-weight: bold;
  color: #63b3ed; /* text-blue-400 */
  font-family: 'Press Start 2P', cursive;
  margin: 0;
}

/* Formulario */
.register-form {
  margin-top: 1.5rem; /* mt-6 */
}

.register-form label {
  display: block;
  margin-bottom: 0.5rem; /* mb-2 */
  color: #90cdf4; /* text-blue-300 */
  font-family: 'Press Start 2P', cursive;
}

/* Aplica margen superior a los labels que no sean el primero */
.register-form label:not(:first-of-type) {
  margin-top: 1rem;
}
.register-form input[type="text"],
.register-form input[type="email"],
.register-form input[type="password"] {
  width: 100%;
  padding: 0.5rem; /* p-2 */
  border-radius: 0.25rem; /* rounded */
  background-color: #4a5568; /* Gray-700 */
  color: #ffffff;
  border: 1px solid #63b3ed; /* border-blue-400 */
  outline: none;
  box-sizing: border-box;
}

.register-form input[type="text"]:focus,
.register-form input[type="email"]:focus,
.register-form input[type="password"]:focus {
  box-shadow: 0 0 0 2px #68d391; /* focus:ring-2 y focus:ring-green-400 */
}

/* Botón */
.register-button {
  width: 100%;
  margin-top: 1.5rem;
  background-color: #4299e1; /* blue-500 */
  color: #1a202c; /* text-gray-900 */
  font-weight: bold;
  padding: 0.5rem 1rem; /* py-2 px-4 */
  border-radius: 0.5rem; /* rounded-lg */
  border: none;
  cursor: pointer;
  transition: background-color 0.2s ease-in-out;
}

.register-button:hover {
  background-color: #63b3ed; /* blue-400 */
}

/* Enlace para iniciar sesión */
.register-paragraph {
  text-align: center;
  margin-top: 1rem; /* mt-4 */
  color: #68d391; /* text-green-400 */
  font-family: 'Press Start 2P', cursive;
}

.register-paragraph a {
  text-decoration: underline;
  color: #68d391;
}

.register-paragraph a:hover {
  color: #9ae6b4; /* hover:text-green-300 */
}
.error-message {
  background-color: #e53e3e;
  color: white;
  padding: 10px;
  border-radius: 5px;
  margin-bottom: 10px;
  text-align: center;
}

.spinner {
  border: 4px solid rgba(255, 255, 255, 0.3);
  border-top: 4px solid white;
  border-radius: 50%;
  width: 30px;
  height: 30px;
  animation: spin 1s linear infinite;
  margin: auto;
}

.hidden {
  display: none;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
  </style>
</head>
<body>
  <div class="register-container">
    <h1>⚽ Retro Football Registration</h1>

    <!-- Error messages -->
    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>

    <!-- Loading Spinner -->
    <div id="loading-spinner" class="spinner hidden"></div>
    <!-- Registration Form -->
    <form action="/auth/register" method="POST" class="register-form">
      <label for="email">Email</label>
      <input type="email" id="email" name="email" required>

      <label for="nickname">Nickname</label>
      <input type="text" id="nickname" name="nickname" required>

      <label for="password">Password</label>
      <input type="password" id="password" name="password" required>

      <label for="confirmPassword">Confirm Password</label>
      <input type="password" id="confirmPassword" name="confirmPassword" required>
      
      <button type="submit" class="register-button">Create Account</button>
    </form>
    <!-- Link to login -->
    <p class="register-paragraph">
      Already have an account? <a href="/auth/login">Log in</a>
    </p>
  </div>
</body>
</html>